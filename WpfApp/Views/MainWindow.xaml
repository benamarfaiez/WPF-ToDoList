<Window x:Class="WpfApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WpfApp.ViewModels"
        xmlns:local="clr-namespace:WpfApp.Views"
        mc:Ignorable="d"
        Title="Gestionnaire de Tâches" Height="450" Width="800"
        WindowStartupLocation="CenterScreen" Background="#F4F6F8">

    <Window.DataContext> <!--Assigne l'instance ci-dessousà la propriété DataContext de la fenêtre Window.-->
        <vm:MainViewModel /> <!--Crée une nouvelle instance de la classe MainViewModel-->
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <!-- diviser le conteneur <Grid> en 3 lignes horizontales -->
            <RowDefinition Height="Auto" />
            <!-- Mode de hauteur "Auto" : La ligne s'adapte dynamiquement aux éléments placés à l'intérieur-->
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <!-- Mode de hauteur "*" :  La ligne occupe l'espace disponible non consommé par les lignes Auto ou fixes.-->
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Titre -->
            <DockPanel x:Name="Titre" LastChildFill="True" Margin="0,0,10,0">
                <Grid DockPanel.Dock="Top">
                    <Border Background="LightGray" Margin="0,0,-157,0">
                        <TextBlock Text="Ma Liste de Tâches" FontSize="20" FontWeight="Bold" Foreground="#2C3E50" 
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </DockPanel>
        
            <!-- Badges dynamique -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <!-- Badge "En cours" (Bleu) -->
                <Border Background="#FF676968" CornerRadius="5" Padding="8,4" Margin="0,0,6,0">
                    <TextBlock Foreground="White" FontSize="12" FontWeight="Bold">
                <Run Text="{Binding PendingCount, Mode=OneWay}"/>
                <Run Text=" en cours"/>
                    </TextBlock>
                </Border>

                <!-- Badge "Terminées" (Vert) -->
                <Border Background="#FF676968" CornerRadius="5" Padding="8,4">
                    <TextBlock Foreground="White" FontSize="12" FontWeight="Bold">
                <Run Text="{Binding CompletedCount, Mode=OneWay}"/>
                <Run Text=" faites"/>
                    </TextBlock>
                </Border>
            </StackPanel>
        </Grid>
        
        <!-- Formulaire de Saisie -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Row="1" Grid.Column="0" Style="{StaticResource RoundedTextBoxStyle}"
                     Text="{Binding NewTaskTitle, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            </TextBox>

            <Button Grid.Column="1" Content="Ajouter" Command="{Binding AddTaskCommand}"
                    Margin="10,0,0,0" Padding="15,8" FontSize="14" FontWeight="SemiBold"
                    Cursor="Hand" Style="{StaticResource RoundedButtonStyle}">
            </Button>
        </Grid>

        <!-- Liste des Tâches -->
        <ListBox x:Name="ListeTaches" Grid.Row="2" ItemsSource="{Binding Tasks}"
                 ItemContainerStyle="{StaticResource CardListBoxItemStyle}"
                 ItemTemplate="{StaticResource TodoItemTemplate}"
                 HorizontalContentAlignment="Stretch">
        </ListBox>
    </Grid>
</Window>
