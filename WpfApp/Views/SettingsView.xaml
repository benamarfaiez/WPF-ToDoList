<UserControl x:Class="WpfApp.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel Margin="20">
        <TextBlock DockPanel.Dock="Top" Text="Paramètres" FontSize="20" FontWeight="Bold" Margin="0,0,0,16" Foreground="{DynamicResource TexteBrush}"/>

        <StackPanel DockPanel.Dock="Top">

            <TextBlock Text="Apparence" FontWeight="Bold" Margin="0,0,0,6" Foreground="{DynamicResource TexteBrush}"/>
            <CheckBox Content="Mode sombre" IsChecked="{Binding ModeSombre}" Margin="0,0,0,12" Foreground="{DynamicResource TexteBrush}"/>

            <TextBlock Text="Données" FontWeight="Bold" Margin="0,0,0,6" Foreground="{DynamicResource TexteBrush}"/>
            <Border Background="{DynamicResource FondSecondaireBrush}" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                <TextBlock Text="{Binding CheminFichierDonnees, StringFormat='Fichier de données : {0}'}"
                           TextWrapping="Wrap" Foreground="{DynamicResource TexteBrush}"/>
            </Border>
            <Button Content="Réinitialiser toutes les tâches" Command="{Binding ReinitialiserDonneesCommand}"
                    HorizontalAlignment="Left" Padding="10,4" BorderThickness="0"
                    Style="{StaticResource RoundedButtonStyle}"
                    Background="{DynamicResource DeleteBrush}" Foreground="{DynamicResource HeaderTextBrush}"/>

        </StackPanel>
    </DockPanel>
</UserControl>
